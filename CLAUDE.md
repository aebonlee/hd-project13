# [Claude Code 작업 지시서] J1939 CAN Protocol 사양 정리 자동화

- 기획자: 안상욱 (HD건설기계, 전기전자검증팀)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] — 수강생 프로젝트 기획서 13번
- 프로젝트 유형: 문서 파싱(Word·PDF·Excel) + 프로토콜 계산 + 엑셀 산출
- 대체 대상: **전장품별로 따로 오는 CAN 사양서를 사람이 눈으로 옮겨 적어 한 표로 모으던 일**

> ⚠ **이 문서는 기획서 원문이 아니라 저장소에서 되짚어 쓴 것이다.**
> 다른 프로젝트의 작업 지시서는 기획서를 먼저 받아 적은 것이지만, 이 저장소는
> 구현이 먼저 있었고 지시서가 없었다. 그래서 §1~§5 는 **README·커밋 메시지·
> 테스트에서 실제로 확인한 것만** 옮겼고, **부록 A(기획서 원문)는 비워 두었다.**
> 지어내면 다음 사람이 그것을 기획자의 말로 믿는다 — 이 프로젝트가 값을
> 지어내지 않는 것과 같은 이유다.

---

## 0. Claude Code에게 — 작업 방식

1. 이 도구의 값어치는 화면이 아니라 **읽기를 틀리지 않는 것**에 있다.
   CAN 사양이 한 줄 틀리면 그 표를 믿고 개발한 전장품이 통신하지 못한다.
   빠르게 많이 읽는 것보다 **모르는 것을 모른다고 남기는 것**이 우선이다.
2. 규칙은 `js/j1939.js`(계산)와 `js/mapping.js`(열 이름)에 모으고
   `test/logic.test.js` 로 고정한다. 화면(`js/app.js`)은 판정하지 않는다.
3. 업체 양식이 새로 들어오면 **코드가 아니라 표를 늘린다** —
   `mapping.js` 의 `ALIASES` 에 이름을 더한다.
4. **사내 실데이터를 저장소에 넣지 않는다.** 예제는 `scripts/make_samples.py`
   가 만든 가짜 사양서 3종을 쓴다.
5. 파일은 서버로 올라가지 않는다. 읽기도 엑셀 만들기도 브라우저 안에서 끝난다.

---

## 1. 한 줄 요약

전장품마다 **따로, 다른 양식으로, 다른 형식(Word·PDF·Excel)으로** 오는 CAN 사양서를
올리면 **ID · PGN · CAN Channel · Source Address · Period** 를 뽑아
**엑셀 한 장**으로 모으는 도구.

## 2. 배경 및 현재 업무 상황

- 건설장비는 **모델별 CAN 사양**을 관리해야 한다.
- 그런데 사양은 **전장품별로 따로** 들어온다. 업체마다 양식이 다르고
  형식도 Word·PDF·Excel 로 제각각이다.
- 지금은 사람이 눈으로 읽어 한 표에 옮겨 적는다. 옮기다 한쪽만 고치면
  **ID 와 PGN 이 서로 어긋난 표**가 만들어지는데, 표만 봐서는 알 수 없다.

## 3. 저장소에서 확인한 것

### 3.1 정리하는 다섯 항목

| 항목 | 형식 | 예 |
|---|---|---|
| ID | 16진 7~8자리 | `0x18F00401` · `0xCF00400` |
| PGN | 16진 4자리, 접두어 없이 | `E000` · `F029` · `FDD8` |
| CAN Channel | 1~3 (10진) | `2` |
| Source Address | 16진 2자리, 접두어 없이 | `21` · `44` · `5A` |
| Period | ms. 요청 시 전송은 `0` | `100` · `1000` · `0` |

### 3.2 ID 가 PGN 의 상위다

**ID 안에 PGN 이 들어 있다.** 그래서 한쪽만 있어도 채울 수 있는 것과 없는 것이 갈린다.

| 사양서에 있는 것 | 채울 수 있는 것 | 왜 |
|---|---|---|
| ID | PGN · Source Address · 우선순위 | ID 를 뜯으면 그 안에 다 있다 |
| PGN | **아무것도** | 우선순위와 SA 를 모른다 → **ID 를 만들지 않는다** |
| CAN Channel · Period | 없음 | 계산으로 얻을 수 없다 |

PDU1(목적지 있는 메시지)에서는 **PS 를 PGN 에 넣지 않는다.** PS 는 목적지 주소이지
PGN 의 일부가 아니다. 이걸 틀리면 표 전체가 조용히 어긋난다.

### 3.3 세 형식을 어떻게 읽나

| 형식 | 방법 |
|---|---|
| Excel | SheetJS 로 시트마다 표를 읽는다 |
| Word | `.docx` 는 ZIP 이다. 브라우저 내장 `DecompressionStream` 으로 풀어 `word/document.xml` 의 표를 읽는다 — **압축 라이브러리를 들이지 않았다** |
| PDF | pdf.js 로 글자와 **좌표**를 받아, y 가 비슷한 것끼리 한 줄로 묶고 머리글의 x 위치로 칸을 가른다 |

셋 다 표로 만든 뒤는 **같은 코드**를 쓴다(`mapping.js` → `j1939.js`).

> PDF 에 한글이 있으면 **CMap 이 필요하다.** 내장되지 않은 CID 글꼴로 그린 사양서가
> 흔한데, CMap 없이 읽으면 한글이 **빈 문자열**로 나와 "표가 없다"가 된다.
> `lib/cmaps/` 를 함께 두는 이유다.

---

## 4. 반드시 규칙으로 막을 것 (테스트로 고정)

`test/logic.test.js` 31개가 지킨다. ★ 는 틀리면 표 전체가 조용히 망가지는 것.

| # | 규칙 | 어디서 |
|---|---|---|
| 1 | ★ **PGN 만 있으면 ID 를 지어내지 않는다** | `J1939.complete` |
| 2 | ★ PDU1 에서 PS 를 PGN 에 넣지 않는다 | `J1939.decodeId` |
| 3 | ★ 문서의 PGN·SA·우선순위가 ID 계산값과 어긋나면 **양쪽을 다 적어 알린다.** 고치지 않는다 | `J1939.crossCheck` |
| 4 | ★ 머리글을 못 찾으면 **추측해 붙이지 않는다** | `Mapping.findHeaderRow` · `mapHeader` |
| 5 | 계산으로 채운 값에는 **「계산」 표시**를 단다 | `J1939.complete` + 화면 |
| 6 | ID·PGN 이 둘 다 없으면 알린다 | `J1939.complete` |
| 7 | SA 는 **16진** 2자리다 — 10진으로 읽으면 안 된다 | `J1939.parseSa` |
| 8 | CAN 채널은 1~3 만. 밖이면 비우고 알린다 | `J1939.parseChannel` |
| 9 | 요청 시 전송(`0`)과 빈칸을 구분한다 | `J1939.parsePeriod` |
| 10 | 신호가 데이터 길이(DLC)를 넘으면 잡는다 | `J1939.crossCheck` |
| 11 | 시작비트를 0 부터 센 문서를 잡는다 (J1939 는 1 부터) | `J1939.crossCheck` |
| 12 | 29비트를 넘는 ID 는 받지 않는다 | `J1939.parseId` |

### 왜 ID 를 지어내지 않는가

PGN 만 있을 때 우선순위 6 을 가정하면 **그럴듯한 ID** 가 나온다.
그런데 그 값은 사양이 아니라 **추측**이다. 한 번 엑셀에 들어가면
다음 사람은 그것을 실제 사양으로 믿고 쓴다. 그래서 비워 둔다.

### 왜 고쳐 주지 않는가

ID 와 PGN 이 둘 다 적힌 사양서에서 둘이 어긋나면, **어느 쪽이 맞는지는 원본을
봐야 안다.** 계산값으로 덮으면 문서가 틀린 것인지 우리가 틀린 것인지
아무도 모르게 된다. 「검토필요」에 양쪽 값을 다 적는다.

---

## 5. 산출물

- 빌드 없이 도는 순수 HTML/CSS/JS (GitHub Pages 배포)
- `test/logic.test.js` — 규칙 31개
- `test/smoke.browser.js` — 브라우저에 실제로 띄워 17개
- `scripts/make_samples.py` — 예제 사양서 3종 (서로 다르게 만든다)
- `README.md`, `docs/개발일지.md`

예제를 일부러 다르게 만든 이유가 있다. 셋이 같으면 **한 가지만 맞춰도 통과**해
검사가 의미를 잃는다.

| 예제 | 특징 |
|---|---|
| `A사_…xlsx` | **ID 만** 있음 → PGN·SA 를 계산해야 함 |
| `B사_…docx` | **PGN·SA·채널만** 있음 → ID 를 지어내면 안 됨. CAN 표가 아닌 부속 표도 섞여 있음 |
| `C사_…pdf` | 다섯 항목 다 있음. 영문 머리글 + **한 줄이 일부러 어긋나 있음** |

## 6. 아직 못 하는 것

- **문장으로 적힌 사양** — 사양서 대부분이 표라 표부터 했다
- **스캔한 PDF** — 글자층이 없어 못 읽는다. OCR 이 필요하다
- 모델·전장품을 **파일명에서 추측하지 않는다.** 사양서 안에 열로 있으면 담고 없으면 비운다

---

## 부록 A. 기획서 원문

> **비어 있음.**
>
> 이 저장소에는 기획자(안상욱)의 기획서 원문이 남아 있지 않다.
> 다른 12개 프로젝트의 작업 지시서는 이 자리에 원문을 그대로 싣고 있고,
> 그 원문이 최종 기준 역할을 한다.
>
> **채우는 방법**: 기획서 원문을 받아 이 아래에 인용(`>`)으로 그대로 붙인다.
> 붙인 뒤 §1~§4 가 원문과 어긋나는 곳이 없는지 확인한다 —
> 지금 적힌 것은 구현에서 되짚은 것이라, 기획자가 요구했지만 구현되지 않은
> 항목은 여기 없을 수 있다.
>
> 요약하거나 다시 쓰지 말 것. 원문 그대로가 기준이다.
